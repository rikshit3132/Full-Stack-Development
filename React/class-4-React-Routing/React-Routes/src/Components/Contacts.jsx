import React from 'react'

const Contacts = () => {
    // const path = window?.location?.pathname;
    // console.log(path);
    // return <>{path === '/contact' && <div>Contacts</div>}</>;
    return(
        <div>Contact</div>
    )
}

export default Contacts